app:
  build:
    context: .
    dockerfile: Dockerfile
  container_name: smart-rabbit-app
  restart: always
  env_file:
    - .env   # <-- This ensures the app container gets all env vars
  ports:
    - "3005:3005"
  depends_on:
    - db

version: "3.8"

services:
  smart-rabbit:
    build: .
    container_name: smart-rabbit
    restart: unless-stopped
    ports:
      - "3005:3005"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
